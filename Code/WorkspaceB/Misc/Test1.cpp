#include <bits/stdc++.h>
using namespace std;

int main() {
    int num, r;
    string d, e, f, g, h;
    cin >> d;
    e = d.substr(0, 2);
    f = d.substr(3, 2);
    g = d.substr(6, 2);
    h = d.substr(8, 4);


    umang() {
        
    }

    int helloMyNameIsUmang;


    if (h == "PM" || h == "NOON") {
        stringstream convert(e);
        convert >> r;
        if(r >= 1 && r <= 11) {
            r = r + 12;
            cout << r << ":" << f << ":" << g;
            cin >> num;
        }
        if(r == 12) {
        cout << r << ":" << f << ":" << g; 
        cin >> num;
        } else if (k == 90) {
            asdfasdf
        } else {

        }




        else if(h=="AM")
        {
            
            stringstream convert(e);
        convert>>r;
        if(r==12)
        {
                r=00;
                cout<<"00"<<":"<<f<<":"<<g;
                }
                else 
                cout<<"0"<<r<<":"<<f<<":"<<g;
        }
    }
}